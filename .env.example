# Copy to .env and fill in. Used by docker-compose.tunnel.yml.
#
# Connector token for the Cloudflare Tunnel, from the Zero Trust dashboard:
# Networks > Tunnels > (your tunnel) > Configure > Docker. It is a credential -
# .env is gitignored, keep it that way.
TUNNEL_TOKEN=
